server:
  port: 8001

spring:
  application:
    name: demoitp

  datasource:
    url: jdbc:mysql://localhost:3306/demoitpdb
    username: root
    password: My$tr0ngP@ssw0rd!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    show-sql: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
